export type TProduct = {
  _id: string;
  name: string;
  image: string;
  price: string;
  reviews: string;
  description: string;
  originalPrice?: string;
  badgeColor?: string;
  discount?: string;
  rating: number;
  badge?: string;
};
